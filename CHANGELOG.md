# Changelog

## Unreleased

- Product Miner onboarding now supports private `crowdtensor_miner_join_invite_v1` files/codes generated by `scripts/create_miner_invite.py --invite-file`, and `crowdtensor join --invite-file/--invite` applies the Coordinator URL, Miner identity, stage, backend, model id, token, and policy limits while redacting plaintext tokens from reports. Coordinator `/ready` now exposes a safe `crowdtensor_miner_registry_policy_summary_v1` for registry `join_policy` metadata. This is admission/audit metadata for the current Beta, not production billing, staking, or automatic incentives.
- Public Swarm GPU Inference Beta and GPU Sharded Generation Beta now surface top-level `output_request`, `answer_scope.scope_state: no-local-answer`, and `shareable_summary.answer_scope_state` in redacted JSON, Markdown, terminal summaries, and their CI-safe checks.
- Public Swarm Inference Preview v0.4 now surfaces top-level `output_request`, `answer_scope.scope_state: no-local-answer`, and `shareable_summary.answer_scope_state` in redacted JSON, Markdown, terminal summaries, and Support Bundle output for `preview-v04 local-smoke/package/evidence-import`.
- Public Swarm v0.2 Usable Inference Trial now surfaces top-level `output_request`, `answer_scope.scope_state: no-local-answer`, and `shareable_summary.answer_scope_state` in redacted JSON, Markdown, terminal summaries, and Support Bundle output for `swarm-trial local-loopback/package/live-kaggle/evidence-import`.
- Public Swarm v0.1 Operator Preview now surfaces top-level `output_request`, `answer_scope.scope_state: no-local-answer`, and `shareable_summary.answer_scope_state` in redacted JSON, Markdown, terminal summaries, and Support Bundle output for `operator-preview local-smoke/package/live-kaggle/evidence-import`.
- Public Swarm Developer Preview now surfaces top-level `output_request`, `answer_scope.scope_state: no-local-answer`, and `shareable_summary.answer_scope_state` in redacted JSON, Markdown, terminal summaries, and Support Bundle output for the ordinary `preview local/package/external-existing/evidence-import` aggregate.
- Public Swarm Product Beta now surfaces top-level `output_request`, `answer_scope.scope_state: no-local-answer`, and `shareable_summary.answer_scope_state` in redacted JSON, Markdown, terminal summaries, and Support Bundle output for the ordinary `serve` / `join` / `generate` product-path aggregate.
- Public Real-LLM Swarm Beta now surfaces top-level `output_request`, `answer_scope.scope_state: no-local-answer`, and `shareable_summary.answer_scope_state` in redacted JSON, Markdown, terminal summaries, and Support Bundle output so the top-level installable aggregate is clearly shareable evidence rather than a local answer transcript.
- Public Swarm Inference v2 now surfaces top-level `output_request`, `answer_scope.scope_state: no-local-answer`, and `shareable_summary.answer_scope_state` in redacted JSON, Markdown, terminal summaries, and Support Bundle output so operators can distinguish shareable readiness evidence from local human `generate` answers.
- Added Public Swarm Inference v2 (`crowdtensor public-swarm-v2`, `public_swarm_inference_v2`, `scripts/public_swarm_inference_v2_pack.py`, `scripts/public_swarm_inference_v2_check.py`) as the current strongest public-preview artifact over the ordinary `p2pd` / `serve --p2p` / distinct `join --p2p stage0|stage1` / `generate --p2p` flow. The local gate now targets 16 generated tokens, P2P route discovery, real small Hugging Face split inference, 32 accepted stage rows, stage rescue/requeue evidence, latency/throughput/memory summaries, model-consistent local/external/P2P evidence, optional CUDA evidence or fail-closed diagnostics, redacted Markdown/support artifacts, and explicit Coordinator-backed/read-only/tiny-or-small-model/not Hivemind-Petals production parity/not Coordinator-free/not NAT traversal production/not large-model boundaries. `--fresh-external-report` marks a just-run external real-P2P/Kaggle report as fresh external v2 validation only when it also exposes the expected external accepted stage rows (`2 * max_new_tokens`); otherwise external evidence is reported as retained evidence plus an actionable runbook. All v2 imports now require explicit matching `hf_model_id` metadata, including the default tiny-GPT path, or v2 blocks with model mismatch diagnostics.
- Added Usable Swarm Inference v1 (`crowdtensor usable-swarm`, `usable_swarm_inference_v1`, `scripts/usable_swarm_inference_pack.py`, `scripts/usable_swarm_inference_check.py`) as the ordinary user-facing path over `crowdtensor p2pd`, `serve --p2p`, distinct `join --p2p --stage stage0|stage1` Miners, and `generate --p2p --max-new-tokens 8`. The maintainer gate requires P2P route discovery, real small HF split generation, distinct stage Miners, accepted rows for both stages, local stage rescue evidence, `usable_swarm_model_match_ready`, top-level `output_request`, `answer_scope.scope_state: no-local-answer`, `shareable_summary.answer_scope_state`, redacted Support Bundle output, and `USABLE_SWARM_INFERENCE.md` while preserving explicit Coordinator-backed/read-only/tiny-or-small-model/not Hivemind-Petals production parity/not Coordinator-free/not NAT traversal production/not large-model boundaries. Non-default `--hf-model-id` imports now require matching P2P v0.6 `hf_model_id` metadata.
- Product `infer` / `generate` / `join` fallback guidance now keeps token and peer-secret values as `CROWDTENSOR_...` environment requirements in printed next commands, while JSON/Markdown reports stay redacted. P2P-lite fallback still prints `crowdtensor p2pd`; `--p2p-backend real` fallback now prints the matching `crowdtensor p2p-daemon`.
- `crowdtensor generate` now shows local generated text by default for human non-JSON runs; JSON and public artifacts still suppress raw generated text and expose safe hashes/counts only.
- Added Public Real-LLM Swarm Inference Beta v1 (`crowdtensor public-real-llm-swarm-beta`, `public_real_llm_swarm_beta_v1`, `scripts/public_real_llm_swarm_beta_pack.py`, `scripts/public_real_llm_swarm_beta_check.py`) as the top-level installable inference Beta aggregate over Product Beta, retained external real-LLM two-stage requeue evidence, retained real-P2P candidate evidence, retained Usable Swarm KV-cache evidence, optional GPU evidence, CUDA fail-closed smoke, redacted Markdown, and Support Bundle output. Ready reports preserve `public_real_llm_swarm_beta_ready`, `cpu_default_ready`, `external_two_stage_ready`, `external_stage_requeue_ready`, `p2p_ready_product_beta`, `p2p_live_requeue_rescue_ready`, `p2p_victim_result_not_accepted`, `public_real_llm_swarm_beta_product_model_match_ready`, `public_real_llm_swarm_beta_kv_cache_ready`, `public_real_llm_swarm_beta_kv_cache_model_match_ready`, `cuda_optional_fail_closed_ready`, and `release_evidence_ready` while keeping explicit Coordinator-backed/read-only/tiny-or-small-model/not full Hivemind-Petals parity/not Coordinator-free/not NAT traversal production/not large-model boundaries. Non-default `--hf-model-id` release aggregates now require product, external, P2P, and KV-cache evidence to expose a matching `hf_model_id` instead of reusing retained tiny-GPT evidence.
- Added the real libp2p discovery alpha backend for `crowdtensor p2p-daemon --discovery-backend libp2p-kad`, using `scripts/libp2p_kad_daemon.mjs`, stable peer identity, TCP/noise/yamux, bootstrap peers, provider-record stream sync, and Kad peer-routing diagnostics while preserving the `http-provider-store` fallback.
- Added `scripts/libp2p_discovery_alpha_check.py` plus `crowdtensor real-p2p-rc` support for `kaggle-connectivity`, diagnostic-only `kaggle-runtime-smoke`, and cleanup-backed `kaggle-auto` external generation; retained libp2p local evidence is under `dist/real-p2p-libp2p-local-smoke-ready`, Kaggle runtime smoke evidence is under `dist/real-p2p-libp2p-kaggle-runtime-smoke-20260531-r6`, and the retained external stage0/stage1 libp2p generation proof is under `dist/real-p2p-libp2p-kaggle-auto-20260531-r4`. Evidence imports now preserve `real_p2p_core_rc_model_metadata_ready` and reject non-default `--hf-model-id` imports that reuse mismatched retained reports with `real_p2p_core_rc_model_metadata_mismatch`.
- Real P2P Core RC live `external-existing --verify-generate` now forwards `--hf-model-id`, bounded `--prompt-texts`, and `--stream-generation` into nested `crowdtensor generate` and records safe model/batch/stream summaries; local and Kaggle Real P2P generate commands now pass the requested model id into the actual generation process.
- Petals-class P2P candidate and Public Real-LLM Swarm Beta imports now preserve safe Real P2P batch/stream summaries and surface P2P-side batch/stream readiness codes without exposing raw prompts, generated text, or token ids.
- Public Real-LLM Swarm Beta `release` now fresh-runs the Petals-class P2P candidate local-smoke, Public Swarm Inference v2 local P2P evidence, and top-level KV-cache readiness from the fresh `public-swarm-v2/usable-v1-local/usable_swarm_inference.json` child report; `evidence-import` still accepts `--p2p-report` and `--public-swarm-v2-report`. The top-level aggregate now preserves `public_real_llm_swarm_beta_public_swarm_v2_ready`, `public_real_llm_swarm_beta_p2p_user_path_ready`, `public_real_llm_swarm_beta_v2_batch_ready`, `public_real_llm_swarm_beta_v2_stream_ready`, `public_swarm_v2_local_p2p_generate_ready`, `public_swarm_v2_16_token_generation_ready`, `public_swarm_v2_external_stage_rows_ready`, `public_swarm_v2_dual_stage_kv_cache_ready`, `public_swarm_v2_model_match_ready`, `public_swarm_v2_signed_or_real_p2p_ready`, `public_swarm_v2_stage_requeue_rescue_ready`, and `public_real_llm_swarm_beta_private_artifacts_cleaned`; missing v2 token, stage-row, batch, stream, model evidence, or final private-artifact cleanup now blocks the top-level Beta. Fresh local release evidence is `dist/goal-final-infer-public-real-llm-swarm-beta-release-fresh-v2-usable-p2p-fixed-20260602/public_real_llm_swarm_beta.json` with fresh product, release-local P2P candidate, v2 local, release-local Usable/KV-cache steps, `source_reports.p2p_report` pointing to `p2p-candidate/petals_class_p2p_candidate.json`, retained external/GPU imports, retained P2P source inputs, and no generated runtime-private files in the final release tree.
- Real P2P local-smoke process supervision now spools long-running Coordinator and daemon output to temp files instead of unbounded pipes, kills process groups on timeout, and prunes child `state/` plus peer-key files before public P2P candidate reports are persisted.
- Public Swarm Inference Beta RC and Public Swarm Product Beta now accept `--stream-generation`, forward it into `crowdtensor generate --stream`, preserve safe stream progress summaries, and emit `public_swarm_generate_stream_ready` / `public_swarm_generate_stream_endpoint_ready` on the ordinary product path.
- Public Real-LLM Swarm Beta now forwards `--stream-generation` through the Product Beta path, preserves product-path safe stream summaries, and emits `public_real_llm_swarm_beta_stream_ready` plus shared stream readiness codes without exposing raw prompts, generated text, or token ids.
- Added strict Real Internet Swarm Inference Beta `evidence-import` support for retained generation plus retained live requeue reports. The import gate now requires safe `generated_token_count >= --max-new-tokens`, matching model metadata, cleanup evidence, imported backend/schema metadata, and public-safe victim/rescue details before emitting `real_llm_internet_beta_evidence_import_ready`; retained 16-token import evidence is `dist/goal-final-infer-real-llm-internet-beta-import-16tok-gpu-summary-20260602/real_llm_internet_beta.json`.
- Added a retained 16-token Public Real-LLM Swarm Beta release-grade import proof at `dist/goal-final-infer-public-real-llm-swarm-beta-import-16tok-p2p-batch-stream-kv-cache-model-gated-v2-20260602/public_real_llm_swarm_beta.json`, combining the local Product Beta batch/stream proof, Public Swarm v2 ordinary P2P user-path evidence, product/external/P2P/v2/KV-cache model match readiness, external 16-token target readiness, P2P 16-token target readiness, v2 32 accepted local/external stage rows, v2 batch/stream readiness, external stage requeue, P2P live victim/rescue requeue, P2P-side batch/stream readiness, persistent dual-stage KV-cache reuse with 15 stage0 and 15 stage1 hits, optional GPU evidence, and redacted support artifacts with no `not_completed` items. The default retained external/P2P/v2/Usable import sources now point at the 16-token Real Internet Beta import, the composed 16-token Petals-class P2P candidate with supplemental batch/stream evidence, `dist/public-swarm-inference-v2/public_swarm_inference_v2.json`, and the 16-token Usable Swarm KV-cache proof. Product, external, P2P, v2, and KV-cache evidence must expose the requested model id; mismatches now block the release aggregate with model mismatch diagnostics. This is retained evidence aggregation, not a fresh external run.
- Fixed real tiny-GPT sharded activation shape handling so stage-local split inference preserves batched hidden states and stage 1 accepts squeezed activation summaries.
- The first Hivemind/Petals-class Alpha proof now emits `external_libp2p_stage_discovery_ready`, `external_libp2p_generate_ready`, and `hivemind_petals_class_alpha_ready` for two private Kaggle CPU stage Miners using libp2p stream provider discovery and a Coordinator result fallback. This is still not Hivemind/Petals production parity, full Kademlia provider-value storage, NAT traversal, decentralized security, an economic system, or large-model throughput.
- Added Public Swarm Inference Preview v0.4 (`crowdtensor preview-v04`, `public_swarm_preview_v04_v1`, `scripts/public_swarm_preview_v04_pack.py`, `scripts/public_swarm_preview_v04_check.py`) with `local-smoke`, `package`, and `evidence-import` modes over the product `serve` / `join` / `generate` path, retained external stage0/stage1 requeue evidence, retained GPU multi-token generation evidence, stage latency/throughput/memory-or-VRAM summaries, `tiny-gpt2` fallback, optional strict `distilgpt2` / `gpt2`, and readiness codes `public_swarm_preview_v04_ready`, `external_two_stage_generation_ready`, `multi_token_generation_ready`, `distinct_stage_miners`, `stage_assignment_valid`, `stage_latency_ready`, `throughput_summary_ready`, `memory_or_vram_summary_ready`, `external_stage_requeue_ready`, `tiny_gpt2_ci_fallback_ready`, and `optional_distilgpt2_or_gpt2_strict_ready`.
- Retained v0.4 evidence at `dist/public-swarm-preview-v04-final/public_swarm_preview_v04.json` and strict local CPU `distilgpt2` proof at `dist/public-swarm-preview-v04-distilgpt2-strict/public_swarm_preview_v04.json`, with public outputs limited to hashes, counts, timing, memory/VRAM summaries, diagnosis codes, redacted Markdown, and Support Bundle data.
- Added `scripts/product_swarm_mvp_check.py`, a v0.3 MVP smoke that validates the real product `serve` / `join stage0` / `join stage1` / `generate` path with a local Coordinator and two stage Miners when optional `[hf]` dependencies are installed.
- Preserved safe generation counts in sanitized reports while continuing to redact raw generated text, generated token ids, activations, leases, idempotency material, and secrets.
- Documented the v0.3 Coordinator-backed Product Swarm MVP path in README, operations docs, roadmap, and project memory.
- Added P2P Swarm Inference v0.6 (`crowdtensor p2p-swarm-v06`, `p2p_swarm_inference_v06_v1`, `scripts/p2p_swarm_inference_v06_pack.py`, `scripts/p2p_swarm_inference_v06_check.py`) plus top-level `crowdtensor p2pd` and product command flags `serve --p2p`, `join --p2p`, and `generate --p2p --prompt`. The local smoke starts a real P2P-lite daemon, announces Coordinator/stage capabilities, verifies P2P route selection, runs stage rescue rediscovery, preserves Coordinator result fallback, and emits `p2p_swarm_inference_v06_ready`, `p2p_discovery_routing_prototype_ready`, `local_three_process_p2p_discovery_ready`, `p2p_stage_discovery_ready`, `p2p_generate_route_ready`, `p2p_stage_rescue_ready`, `p2p_v06_model_metadata_ready`, `stage0_rescue_peer_discovered`, `stage1_rescue_peer_discovered`, `coordinator_to_p2p_transition_ready`, and `coordinator_result_fallback_ready`. Non-default `--hf-model-id` evidence imports now require matching P2P model metadata and block with `p2p_v06_model_metadata_mismatch` when retained tiny-GPT evidence is reused. The new `external-existing --peer-bootstrap` mode verifies an already-running external P2P bootstrap catalog and can run live `generate --p2p` when `--verify-generate --admin-token` are supplied, emitting `external_existing_p2p_verified` and `external_p2p_runtime_verified` for route-level external proof.
- Retained v0.6 local evidence at `dist/p2p-swarm-inference-v06-local-smoke-refresh2/p2p_swarm_inference_v06.json` and external Kaggle evidence at `dist/p2p-swarm-inference-v06-kaggle-auto-final/kaggle-auto/p2p_v06_kaggle_auto.json`; the live Kaggle proof used two external stage Miners, generated 2 tiny-GPT tokens through `generate --p2p`, accepted 4 ledger rows with distinct stage Miners, deleted the private Kaggle kernels, and requires token rotation. Hosts without optional `[hf]` dependencies now report `p2p_real_generate_hf_runtime_missing` or `host_hf_runtime_missing` instead of claiming real tiny-GPT P2P generation.
- Added Public P2P Swarm Inference v1.0 RC (`crowdtensor public-p2p-v1-rc`, `public_p2p_swarm_inference_v1_rc_v1`, `scripts/public_p2p_swarm_inference_v1_rc_pack.py`, `scripts/public_p2p_swarm_inference_v1_rc_check.py`) with shared-secret HMAC peer identity, signed peer announcements, signed `p2pd` registry enforcement, signed `serve --p2p` / `join --p2p` announcements, signed registry health counts, public runbook, Support Bundle, private Kaggle payload cleanup, model-consistent v0.6 imports, and readiness codes including `public_p2p_swarm_inference_v1_rc_ready`, `signed_peer_announcement_ready`, `peer_identity_ready`, `peer_registry_health_ready`, `public_p2p_v1_rc_model_metadata_ready`, `external_p2p_generate_verified`, and `kaggle_kernels_deleted`. Non-default `--hf-model-id` imports now require matching signed local/external/Kaggle v0.6 `hf_model_id` metadata and block with Public P2P v1 RC model mismatch diagnostics when stale tiny-GPT evidence is reused.
- Retained the fresh signed external Kaggle CPU proof at `dist/public-p2p-v1-rc-kaggle-auto-signed-r2/public_p2p_swarm_inference_v1_rc.json`; it used two private signed stage kernels, generated 2 tiny-GPT tokens via P2P discovery, deleted the Kaggle kernels, removed local private kernel payloads, and requires token plus peer-secret rotation. Stage rescue remains covered by signed local proof and retained external requeue evidence rather than a fresh signed Kaggle victim/rescue run. This is a Petals-style public preview shape, not production Hivemind/Petals parity, libp2p/DHT/NAT traversal, decentralized security, or large-model throughput.

- Added product-level model selection plumbing for split inference: `crowdtensor generate --hf-model-id` now propagates through `session_protocol_v1`, `/admin/inference-sessions`, and StateStore per-session artifact inspection. P2P v0.6 external live verification now forwards model id, bounded prompt batches, and stream requirements into nested `generate` and records safe model/batch/stream evidence.

All notable CrowdTensorD Alpha changes are tracked here.

## 0.1.0a0 - Alpha

CrowdTensorD is currently an experimental control plane for fault-tolerant distributed AI workload Miners. This release is intended for local development, controlled remote demos, and implementation review.

### Added

- Public Swarm Developer Preview (`crowdtensor preview`, `public_swarm_developer_preview_v1`, `scripts/public_swarm_developer_preview_pack.py`, `scripts/public_swarm_developer_preview_check.py`) with `local`, `package`, `external-existing`, and `evidence-import` modes over Product Beta, CPU fallback, support bundle, optional retained GPU generation evidence, `developer_preview_ready`, `public_swarm_developer_preview_ready`, `local_two_stage_generation_ready`, `serve_join_generate_ready`, `product_beta_ready`, `support_bundle_ready`, `cpu_fallback_ready`, `local_cpu_inference_ready`, `gpu_generation_evidence_import_ready`, inherited `hf_dependencies_missing`, and explicit CPU-only-by-default/read-only/Coordinator-backed/not production/not libp2p/not DHT/not NAT traversal/not large-model boundaries.
- Public Swarm Live Preview RC (`crowdtensor live-preview`, `public_swarm_live_preview_rc_v1`, `scripts/public_swarm_live_preview_rc_pack.py`, `scripts/public_swarm_live_preview_rc_check.py`) with `live-preview local-smoke`, `live-preview package`, side-effectful `live-preview live-kaggle`, and `live-preview evidence-import` modes over Developer Preview, Public Swarm Alpha live Kaggle proof, retained Alpha RC evidence, Support Bundle diagnostics, optional retained GPU generation evidence, `public_swarm_live_preview_rc_ready`, `public_swarm_live_preview_local_smoke_ready`, `public_swarm_live_preview_package_ready`, `public_swarm_live_preview_live_kaggle_ready`, `public_swarm_live_preview_evidence_import_ready`, `external_stage_requeue_ready`, `live_stage0_requeue_ready`, `live_stage1_requeue_ready`, `kaggle_kernels_deleted`, `private_artifacts_cleaned`, `token_rotation_required`, `gpu_generation_evidence_import_ready`, fresh retained stage0/stage1 RC reports at `dist/public-swarm-live-preview-rc-live-stage0-20260529043801-rc/public_swarm_live_preview_rc.json` and `dist/public-swarm-live-preview-rc-live-stage1-20260529044328-rc/public_swarm_live_preview_rc.json`, short default Kaggle slug prefix `ct-live-preview`, and explicit CPU-only-by-default/read-only/Coordinator-backed/not production/not libp2p/not DHT/not NAT traversal/not large-model boundaries.
- Public Swarm v0.1 Operator Preview (`crowdtensor operator-preview`, `public_swarm_operator_preview_v1`, `scripts/public_swarm_operator_preview_pack.py`, `scripts/public_swarm_operator_preview_check.py`) with `operator-preview local-smoke`, `operator-preview package`, side-effectful `operator-preview live-kaggle`, and `operator-preview evidence-import` modes over Developer Preview, Live Preview RC, release readiness, Support Bundle diagnostics, CPU fallback, optional retained GPU generation evidence, `public_swarm_operator_preview_ready`, `operator_preview_user_path_ready`, `operator_preview_local_smoke_ready`, `operator_preview_package_ready`, `operator_preview_live_kaggle_ready`, `operator_preview_evidence_import_ready`, `serve_join_generate_ready`, `miner_join_pack_ready`, `cpu_fallback_ready`, `live_preview_ready`, `support_bundle_ready`, `release_readiness_ready`, `gpu_generation_evidence_import_ready`, `developer_preview_degraded`, `operator_preview_cpu_fallback_user_path_ready`, `operator_preview_retained_evidence_ready`, fallback `external_runtime_blocked` when fresh HF/Kaggle/external runtime execution is unavailable, and explicit CPU-only-by-default/read-only/Coordinator-backed/not production/not libp2p/not DHT/not NAT traversal/not large-model boundaries.
- Public Swarm v0.2 Usable Inference Trial (`crowdtensor swarm-trial`, `public_swarm_trial_v1`, `public_swarm_trial_cli_v1`, `scripts/public_swarm_trial_pack.py`, `scripts/public_swarm_trial_check.py`) with `swarm-trial local-loopback`, `swarm-trial package`, side-effectful `swarm-trial live-kaggle`, and `swarm-trial evidence-import` modes over Product Beta, Operator Preview, Support Bundle diagnostics, CPU fallback, and retained GPU generation evidence. Ready reports preserve `public_swarm_trial_ready`, `serve_join_generate_trial_ready`, `stage0_join_ready`, `stage1_join_ready`, `generate_ready`, `generated_token_count_ready`, `support_bundle_ready`, `cpu_fallback_ready`, `private_artifacts_cleaned`, `operator_preview_import_ready`, `gpu_generation_evidence_import_ready`, `swarm_trial_degraded_cpu_fallback_ready`, `external_runtime_blocked`, `token_rotation_required`, and explicit CPU-only-by-default/read-only/Coordinator-backed/not production/not libp2p/not DHT/not NAT traversal/not GPU marketplace/not large-model boundaries.
- Coordinator and Miner loop with task claims, heartbeats, lease timeout recovery, checkpoint replay, and result submission.
- Deterministic CPU-only workload contracts, including dense toy training, CPU LoRA mock, micro Transformer LM, model bundle LM, read-only multi-request model bundle inference, and optional read-only `external_llm_infer_v1` adapter validation.
- Runtime validation for finite values, tensor shape, loss/norm gates, deterministic replay audit, and low-frequency outer optimizer behavior.
- Miner admission controls with shared tokens, per-Miner token registry, hashed token verifiers, observer/admin separation, and security preflight checks.
- Operator views for `/health`, `/version`, `/ready`, `/metrics`, redacted `/state`, admin event tails, and admin result ledger.
- Remote Miner demo flow with invite generation, readiness checks, retry counters, result `idempotency_key`, and controlled remote acceptance.
- Browser experiments for WebRTC tensor transfer, browser compute probes, and browser Miner bridge smoke tests.
- Release tooling: `scripts/release_gate.py`, `scripts/runtime_acceptance_pack.py`, `scripts/browser_acceptance_pack.py`, `scripts/release_evidence_pack.py`, and `scripts/support_bundle.py`.
- Release readiness tooling (`crowdtensor release-ready`, `scripts/release_readiness_pack.py`, `scripts/release_readiness_check.py`) with `release_readiness_v1`, Git metadata aggregation, release gate and security preflight summaries, `demo_manifest_v1` handoff validation, dirty-tree blocker diagnosis such as `git_dirty`, and `--allow-dirty` for development/CI smoke checks only.
- Fresh clone onboarding gate (`scripts/onboarding_gate.py --quick`) with `onboarding_gate_v1`, clean temporary virtualenv creation, `python -m pip install -e .[dev]`, console script checks for `crowdtensor --help`, `crowdtensord --help`, and `crowdtensor-miner --help`, plus smoke validation of `crowdtensor local-proof`, `crowdtensor home-infer`, `crowdtensor llm-infer --mock`, `crowdtensor cpu-infer --mode local`, and `crowdtensor release-ready --allow-dirty` without claiming production readiness.
- Runtime acceptance diagnosis aggregation: per-check safe `summary_json`, top-level `diagnosis_summary`, and release evidence / Support Bundle `diagnosis_by_check` propagation for operator triage.
- Runtime capability matrix (`scripts/runtime_matrix.py`, `scripts/runtime_matrix_check.py`) for CPU-only readiness, optional browser support, external LLM runtime configuration checks, `hardware_targets`, `recommended_routes`, `matched_capabilities`, `missing_capabilities`, route `diagnosis_codes`, `operator_action`, top-level `diagnosis_summary`, and hardware/runtime matrix diagnostics.
- Runtime capability matrix now treats NVIDIA CUDA as an optional tiny GPT split route instead of a pure future placeholder: `nvidia_cuda` may report `nvidia_cuda_optional_missing`, `nvidia_cuda_detected_adapter_unavailable`, or `nvidia_cuda_runtime_ready`, and only a complete CUDA + optional HF runtime recommends `crowdtensor public-swarm-gpu-beta local-loopback`.
- Hardware target diagnosis in `runtime_matrix.py` with target-level `diagnosis_codes`, `operator_action`, matched/missing capabilities, and `hardware_diagnosis_summary` for CPU, NVIDIA, AMD, Apple, browser, remote container, and external LLM targets.
- Matrix-guided home-compute demo (`scripts/home_compute_demo.py`, `scripts/home_compute_demo_check.py`) that pairs runtime capability discovery and `route_decision` with the read-only `model_bundle_infer` session and stable `diagnosis_codes` such as `home_compute_ready` and `runtime_matrix_blocked`.
- Safe, shareable home-compute evidence pack (`scripts/home_compute_evidence_pack.py`, `scripts/home_compute_evidence_check.py`) with `home_compute_evidence_v1`, `matched_capabilities`, `diagnosis_codes`, capped `request_trace`, and runtime acceptance skip flag `--skip-home-compute-evidence`.
- Safe, shareable remote-compute evidence pack (`scripts/remote_compute_evidence_pack.py`, `scripts/remote_compute_evidence_check.py`) with `remote_compute_evidence_v1`, `remote_compute_observability_v1`, `remote_python_model_bundle_infer`, fixed `model_bundle_inference_scenario_v1` metadata, scenario match status, hashed registry status, capped `request_trace`, and runtime acceptance opt-in flag `--include-remote-evidence`.
- Controlled local multi-Miner scenario sweep (`scripts/multi_miner_scenario_sweep.py`, `scripts/multi_miner_scenario_sweep_check.py`) with `multi_miner_scenario_sweep_v1`, `multi_miner_scenario_sweep_observability_v1`, three fixed `model_bundle_inference_scenario_v1` scenarios, registry-backed Python Miner identities, concurrent lease-race mode, `kill-after-claim` failure-requeue mode, `local_multi_miner_model_bundle_infer`, `lease_summary`, `process_summary`, `requeue_summary`, read-only/redaction/hashed-registry safety, `multi_miner_concurrent_ready`, `multi_miner_requeue_ready`, and runtime acceptance opt-in flags `--include-multi-miner-sweep` / `--include-multi-miner-requeue`.
- Safe two-machine remote demo runbook (`scripts/remote_demo_runbook_pack.py`, `scripts/remote_demo_runbook_check.py`) with `remote_demo_runbook_v1`, `operator.private.env`, `miner.private.env`, hashed registry setup, `model_bundle_infer`, and `remote_compute_evidence_pack.py --mode collect --scenario-id route-baseline`.
- Safe two-machine remote demo acceptance pack (`scripts/remote_demo_acceptance_pack.py`, `scripts/remote_demo_acceptance_check.py`) with `remote_demo_acceptance_v1`, `remote_demo_observability_v1`, `--create-session` active task creation through `POST /admin/inference-sessions` with `scenario_id`, `task_id`-bound wait behavior, scenario match verification, `remote_compute_evidence_v1`, `support_bundle` collection, and stable `diagnosis_codes` such as `coordinator_unreachable`, `observer_auth_failed`, `session_create_failed`, and `artifact_collection_failed`.
- Release Evidence and Support Bundle now preserve safe remote `observability_summaries` so issue reports and release artifacts carry `remote_compute_observability_v1` / `remote_demo_observability_v1` without raw tokens, local artifact paths, or raw state dumps.
- Demo Manifest tooling (`scripts/demo_manifest_pack.py`, `scripts/demo_manifest_check.py`) with `demo_manifest_v1`, the local-loopback latest output artifact that indexes `runtime_matrix.json`, `remote_compute_evidence_v1`, `external_llm_evidence_v1`, `support_bundle`, and `remote_compute_observability_v1` summaries.
- User-facing local inference session demo with safe latency, throughput, capped `request_trace`, read-only, redaction, and Miner hardware profile summaries.
- User-facing inference session client (`scripts/inference_session_client.py`) with `inference_session_client_v1`, `session_client_ready`, `POST /admin/inference-sessions`, `task_id`-bound admin ledger polling, safe validation/throughput summaries, `scripts/inference_session_client_check.py`, and runtime acceptance skip flag `--skip-inference-session-client`.
- One-command local proof CLI (`crowdtensor local-proof` in `crowdtensor/cli.py`) with `local_proof_summary_v1`, Doctor/runtime matrix/home-compute demo/Demo Manifest orchestration, CPU-only read-only boundaries, and explicit not production Swarm Inference positioning.
- Home inference proof CLI (`crowdtensor home-infer`) with `home_inference_cli_v1`, `home_compute_evidence_v1` artifact generation, CPU-only read-only `model_bundle_infer`, fixed `model_bundle_inference_scenario_v1` scenarios (`route-baseline`, `gradient-safety`, `mixed-prompts`), capped `request_trace`, `diagnosis_codes`, and explicit not production Swarm Inference positioning.
- External LLM proof CLI (`crowdtensor llm-infer`) with `llm_inference_cli_v1`, `external_llm_evidence_v1` artifact generation through `scripts/external_llm_evidence_pack.py`, deterministic `--mock`, explicit operator-owned `--llm-runtime-cmd` / `--llm-runtime-url` adapters, adapter/model summaries, request/completion counts, output chars, throughput, `external_llm_evidence_ready`, redaction checks, `scripts/external_llm_evidence_check.py`, and runtime acceptance skip flag `--skip-external-llm-evidence`.
- CPU inference Beta aggregate CLI (`crowdtensor cpu-infer`) with `cpu_inference_beta_v1`, `scripts/cpu_inference_beta_pack.py`, `scripts/cpu_inference_beta_check.py`, `--mode local` wrapping `home-infer` plus deterministic `llm-infer --mock`, `--mode remote-loopback` for local `remote-demo` validation, and `--mode remote-existing` for an already running two-machine demo with token/runtime redaction. It is CPU-only, read-only, not production Swarm Inference, not P2P, and not arbitrary prompt serving.
- CPU Inference Beta RC aggregate (`crowdtensor cpu-infer --mode beta-rc`) with `cpu_inference_beta_rc_v1`, `scripts/cpu_inference_beta_rc_pack.py`, `scripts/cpu_inference_beta_rc_check.py`, local CPU inference, remote-loopback inference, Real two-machine CPU inference Beta rehearsal, Kaggle Remote Miner Beta artifacts, `miner_join_pack_v1`, `demo_manifest_v1`, optional `--kaggle-real-runtime-report` import of `kaggle_real_runtime_acceptance_v1`, and readiness codes `cpu_inference_beta_rc_ready`, `local_cpu_inference_ready`, `remote_loopback_ready`, `two_machine_rehearsal_ready`, `kaggle_remote_miner_artifacts_ready`, `miner_join_pack_ready`, `cpu_miner_beta_ready`, and `real_runtime_evidence_ready` when imported. It is CPU-only, read-only, not production Swarm Inference, not P2P, and not a GPU/TPU workload path.
- Pipeline-Sharded Inference Alpha (`crowdtensor shard-infer`) with `sharded_inference_cli_v1`, `sharded_model_bundle_infer`, `sharded_model_bundle_infer_v1`, `sharded_inference_session_v1`, `scripts/sharded_inference_evidence_pack.py`, `sharded_inference_evidence_v1`, `scripts/sharded_inference_check.py`, stage-0 activation hashes/byte counts, stage-1 single-task baseline validation, readiness codes `stage_0_accepted`, `stage_1_accepted`, `activation_transport_ready`, `baseline_match`, `sharded_inference_ready`, and `stage_requeue_ready` for `--failure-mode kill-stage-after-claim`. It is CPU-only, read-only, not production Swarm Inference, not P2P, not GPU/TPU pooling, and not real LLM sharding.
- CPU Pipeline-Sharded Inference Beta (`crowdtensor shard-infer-beta --mode remote-loopback`) with `remote_sharded_inference_beta_v1`, `scripts/remote_sharded_inference_beta_pack.py`, `scripts/remote_sharded_inference_beta_check.py`, mode readiness codes `remote_sharded_inference_ready`, `remote_sharded_loopback_ready`, and `local_sharded_inference_ready`, plus activation hashes, `baseline_match`, and `stage_requeue_ready` for `--failure-mode kill-stage-after-claim`. It is CPU-only, read-only, not production Swarm Inference, not P2P, and not real LLM sharding.
- Remote demo `--workload sharded-model-bundle` with `remote_python_sharded_model_bundle_infer`, `remote_sharded_inference_acceptance_v1`, `remote_sharded_inference_observability_v1`, `remote_two_machine_sharded_ready`, and `scripts/remote_home_compute_demo_check.py --workload sharded-model-bundle`. It is CPU-only, read-only, not production Swarm Inference, not P2P, and not real LLM sharding.
- Micro-LLM Pipeline-Sharded Inference Alpha (`crowdtensor micro-llm-shard-infer`) with `micro_llm_sharded_cli_v1`, `micro_llm_sharded_infer`, `micro_llm_sharded_infer_v1`, `micro_llm_sharded_session_v1`, `scripts/micro_llm_sharded_inference_evidence_pack.py`, `micro_llm_sharded_evidence_v1`, `scripts/micro_llm_sharded_inference_check.py`, stage-0 hidden-state activation hashes, stage-1 deterministic tiny Transformer decode, readiness codes `stage_0_accepted`, `stage_1_accepted`, `activation_transport_ready`, `baseline_match`, `decoded_tokens_match`, `micro_llm_sharded_ready`, and `stage_requeue_ready` for `--failure-mode kill-stage-after-claim`. It is CPU-only, read-only, not production Swarm Inference, not P2P, and not GGUF/llama.cpp or large LLM serving.
- Micro-LLM Artifact Alpha (`crowdtensor micro-llm-artifact`, `scripts/micro_llm_artifact_pack.py`, `scripts/micro_llm_artifact_check.py`) with `micro_llm_artifact_v1`, dependency-free JSON `manifest.json` / `config.json` / `tokenizer.json` / `weights.json`, artifact hash/id/tokenizer propagation into `micro_llm_sharded_infer`, `--micro-llm-artifact` support across local sharded inference, remote-demo, Kaggle real runtime, and live RC paths, plus readiness codes `artifact_loaded` and `micro_llm_artifact_ready`. It is CPU-only, read-only, and not Hugging Face/GGUF/llama.cpp/large-model serving.
- Remote Micro-LLM Pipeline-Sharded Inference Beta (`crowdtensor micro-llm-shard-infer-beta --mode remote-loopback`) with `remote_micro_llm_sharded_beta_v1`, `scripts/remote_micro_llm_sharded_beta_pack.py`, `scripts/remote_micro_llm_sharded_beta_check.py`, mode readiness codes `remote_micro_llm_sharded_ready`, `remote_micro_llm_sharded_loopback_ready`, and `local_micro_llm_sharded_inference_ready`, plus activation hashes, `baseline_match`, `decoded_tokens_match`, and `stage_requeue_ready`. Remote demo `--workload micro-llm-sharded` emits `remote_python_micro_llm_sharded_infer`, `remote_micro_llm_sharded_acceptance_v1`, `remote_micro_llm_sharded_observability_v1`, and `remote_two_machine_micro_llm_sharded_ready`. It is CPU-only, read-only, not production Swarm Inference, not P2P, and not GGUF/llama.cpp serving.
- Stage-aware Micro-LLM split proof with `scripts/stage_aware_micro_llm_sharded_check.py`, `--stage-mode split`, `--require-distinct-stage-miners`, Miner `--micro-llm-stage-role stage0|stage1|both`, capabilities `micro_llm_sharded_stage0`, `micro_llm_sharded_stage1`, `micro_llm_sharded_both`, diagnosis codes `distinct_stage_miners` and `stage_assignment_valid`, plus stage-specific `--failure-mode kill-stage0-after-claim` / `kill-stage1-after-claim` requeue validation.
- Micro-LLM Live Two-Node RC (`crowdtensor micro-llm-live-rc`) with `micro_llm_live_rc_v1`, `scripts/micro_llm_live_rc_pack.py`, `scripts/micro_llm_live_rc_check.py`, `local-generated` stage upload stand-ins using `kaggle-upload-stage0` / `kaggle-upload-stage1`, two independent stage Miner processes, `--micro-llm-artifact` support, readiness codes `local_generated_stage_upload_standins_ready`, `micro_llm_live_rc_ready`, `kaggle_micro_llm_sharded_ready`, `artifact_loaded`, `micro_llm_artifact_ready`, `stage_assignment_valid`, and `external-existing` verification that only reports `external_runtime_verified` for an already running public Coordinator plus two external stage Miners. It is CPU-only, read-only toy two-stage micro-LLM, not production Swarm Inference, not P2P, and not GGUF/llama.cpp serving.
- Real Small-LLM Sharded Inference Beta (`crowdtensor real-llm-shard-infer`, `crowdtensor real-llm-shard-infer-beta --mode remote-loopback`, and `crowdtensor remote-demo --workload real-llm-sharded`) with `real_llm_sharded_cli_v1`, `real_llm_sharded_evidence_v1`, `remote_real_llm_sharded_beta_v1`, `remote_real_llm_sharded_acceptance_v1`, `remote_real_llm_sharded_observability_v1`, `remote_python_real_llm_sharded_infer`, `remote_two_machine_real_llm_sharded_ready`, `scripts/real_llm_sharded_inference_evidence_pack.py`, `scripts/remote_real_llm_sharded_beta_pack.py`, `scripts/remote_real_llm_sharded_beta_check.py`, optional `[hf]` dependencies, `hf_dependencies_missing` diagnostics, `real_llm_sharded_infer` / `real_llm_sharded_infer_v1`, safe `real_llm_artifact_v1` summaries for `sshleifer/tiny-gpt2`, Miner `--enable-hf-tiny-gpt-runtime`, `--hf-cache-dir`, stage roles `--real-llm-stage-role stage0|stage1|both`, capabilities `real_llm_sharded_stage0`, `real_llm_sharded_stage1`, `real_llm_sharded_both`, optional `--real-llm-partition-mode stage-local` module-placement evidence, and readiness codes `real_llm_artifact_ready`, `activation_transport_ready`, `baseline_match`, `decoded_tokens_match`, `stage_assignment_valid`, `real_llm_sharded_ready`, `remote_real_llm_sharded_loopback_ready`, `remote_real_llm_sharded_ready`, `stage_local_partition_ready`, `stage0_partition_loaded`, `stage1_partition_loaded`, and `partition_parameter_split_valid`. It is read-only optional Hugging Face tiny-model evidence, not production Swarm Inference, not P2P, not GPU pooling, not GGUF/llama.cpp serving, and not large-model serving.
- Optional CUDA backend for Real Small-LLM Sharded Inference with explicit `hf_transformers_cuda`, `--real-llm-backend hf_transformers_cuda`, CUDA fail-closed behavior, safe CUDA runtime summaries, and stage capabilities `real_llm_sharded_cuda_stage0`, `real_llm_sharded_cuda_stage1`, and `real_llm_sharded_cuda_both`; the CPU default remains `hf_transformers_cpu`.
- Real Small-LLM Sharded Inference Live RC (`crowdtensor real-llm-live-rc`) with `real_llm_live_rc_v1`, `scripts/real_llm_live_rc_pack.py`, `scripts/real_llm_live_rc_check.py`, generated `kaggle-upload-real-llm-stage0` / `kaggle-upload-real-llm-stage1` packages, `local-generated` HF-enabled stage upload stand-ins, `kaggle-generated` preparation-only mode, `external-existing` verification, readiness codes `local_generated_real_llm_stage_upload_standins_ready`, `real_llm_live_rc_ready`, `remote_real_llm_sharded_ready`, `real_llm_artifact_ready`, `stage_assignment_valid`, `external_runtime_verified`, and `kaggle_real_llm_sharded_ready`. `scripts/kaggle_real_llm_live_package.py` adds `kaggle_real_llm_live_package_v1` private Kaggle dataset/script-kernel packaging with a temporary `--inline-kernel-payload` fallback. A live real-weight Kaggle proof completed against `24.199.118.54:9184` with two private Kaggle CPU script kernels, `kaggle-real-llm-stage0`, `kaggle-real-llm-stage1`, and `sshleifer/tiny-gpt2`, retaining `dist/real-llm-live-goal-external/real_llm_live_rc.json` with `kaggle_real_llm_stage0_seen`, `kaggle_real_llm_stage1_seen`, baseline/decoded-token readiness, distinct stage Miners, and valid stage assignment. Generated launchers preserve `--enable-hf-tiny-gpt-runtime`, `--real-llm-stage-role`, and `launcher_syntax_valid`. It is CPU-only, read-only, not production Swarm Inference, not P2P, not GPU/TPU pooling, not GGUF/llama.cpp serving, and not large-model serving.
- Real Internet Swarm Inference Alpha (`crowdtensor real-llm-internet-alpha`) with `real_llm_internet_alpha_v1`, `scripts/real_llm_internet_alpha_pack.py`, `scripts/real_llm_internet_alpha_check.py`, modes `local-generated`, `package`, and `external-existing`, and readiness codes `real_llm_internet_alpha_ready`, `real_llm_stage_requeue_ready`, `stage_requeue_ready`, `real_llm_live_rc_ready`, `remote_real_llm_sharded_ready`, `real_llm_artifact_ready`, `activation_transport_ready`, `decoded_tokens_match`, `distinct_stage_miners`, and `stage_assignment_valid`. `external_runtime_verified` is only valid for `external-existing` after an already running public Coordinator plus two external stage Miners complete. Reports preserve `token_rotation_required`, CPU-only/read-only semantics, and explicit not production / not P2P / not large-model boundaries.
- First Real Internet Swarm Inference Alpha external proof against `24.199.118.54:9187` with two private Kaggle CPU script kernels, `internet-real-llm-stage0` and `internet-real-llm-stage1`, retaining `dist/real-llm-internet-alpha-external/real_llm_internet_alpha.json` with `ok: true`, `external_runtime_verified`, `real_llm_internet_alpha_ready`, both Kaggle stages seen, baseline/decoded-token match, distinct stage Miners, and valid stage assignment. Temporary Kaggle kernels were deleted after evidence collection and tokens must be rotated after temporary public HTTP proofs.
- Real Internet Swarm Inference Beta (`crowdtensor real-llm-internet-beta`) with `real_llm_internet_beta_v1`, `scripts/real_llm_internet_beta_pack.py`, `scripts/real_llm_internet_beta_check.py`, and side-effectful `kaggle-auto` automation that generates the Alpha package, starts the temporary public Coordinator, pushes private Kaggle CPU script kernels by default or private Kaggle GPU kernels with `--real-llm-backend hf_transformers_cuda`, verifies `external_runtime_verified`, deletes the temporary kernels, stops the Coordinator, and only then reports `real_llm_internet_beta_ready` with `kaggle_kernels_deleted`. The CUDA path preserves CPU Coordinator metadata-only scheduling through `coordinator_cuda_runtime_required: false` and requires torch CUDA on the stage Miners. With `--failure-mode kill-stage0-after-claim` or `kill-stage1-after-claim`, it pushes a target-stage victim kernel, deletes it after Coordinator claim observation, waits for lease timeout requeue, pushes a rescue kernel, and reports `external_stage_requeue_ready`, `live_stage0_requeue_ready` / `live_stage1_requeue_ready`, and `live_requeue_summary`. The check script is CI-safe and fake-runner based. Reports preserve `real_llm_internet_alpha_ready`, both Kaggle stages seen, `decoded_tokens_match`, `distinct_stage_miners`, `stage_assignment_valid`, `token_rotation_required`, CPU-default/read-only semantics, and explicit not production Swarm Inference / not P2P / not GPU pooling / not large-model boundaries.
- Swarm Inference Beta (`crowdtensor swarm-infer-beta`) with `swarm_inference_beta_v1`, `scripts/swarm_inference_beta_pack.py`, `scripts/swarm_inference_beta_check.py`, side-effectful `swarm-infer-beta live` / `kaggle-auto`, `swarm-infer-beta prepare`, `coordinator`, `miner --stage stage0`, `miner --stage stage1`, `verify`, `collect`, and dry-run `clean`. Live wraps `real_llm_internet_beta_v1`, starts a temporary public Coordinator, pushes private Kaggle CPU stage kernels, verifies `external_runtime_verified`, optionally verifies external victim/rescue requeue with `--failure-mode`, deletes kernels, writes `support_bundle.json`, removes local live private artifacts and raw runtime state by default, and only then reports `swarm_inference_beta_live_ready`, `real_llm_internet_beta_ready`, `external_stage_requeue_ready` when requested, `kaggle_kernels_deleted`, `swarm_inference_beta_live_private_artifacts_cleaned`, and `token_rotation_required`. `--keep-live-private-artifacts` is for debugging only. Prepare creates `operator.private.env`, stage0/stage1 `miner.private.env`, hashed `miner_registry.json`, stage join packs, and `SWARM_INFERENCE_BETA.md`; verify wraps `remote_real_llm_sharded_beta_v1`; collect gathers redacted evidence/support; and retained `real_llm_internet_beta_v1` evidence can be imported as `external_beta_evidence_imported`. Ready reports preserve `swarm_inference_beta_ready`, `two_machine_swarm_inference_ready`, `real_llm_split_route_ready`, `decoded_tokens_match`, `distinct_stage_miners`, and `stage_assignment_valid`. It is CPU-only, read-only, not production Swarm Inference, not P2P, not GPU/TPU pooling, and not large-model serving.
- Public Swarm Inference Alpha (`crowdtensor swarm-session`) with `public_swarm_inference_alpha_v1`, `scripts/public_swarm_inference_alpha_pack.py`, and `scripts/public_swarm_inference_alpha_check.py`. `live-kaggle` aggregates cleanup-backed `swarm-infer-beta live` evidence, true external victim/rescue requeue evidence (`external_stage_requeue_ready`, `live_stage0_requeue_ready` / `live_stage1_requeue_ready`, `live_requeue_summary`) when `--failure-mode` is enabled, and mandatory `local-generated` real LLM stage requeue evidence. It reports `public_swarm_inference_alpha_ready`, `public_swarm_session_ready`, `local_stage_requeue_ready`, `public_swarm_live_requeue_ready`, `public_swarm_live_kaggle_ready`, `stage_requeue_ready`, `external_runtime_verified`, `kaggle_kernels_deleted`, and `token_rotation_required`. Child debug artifacts are pruned by default, with `--keep-child-artifacts` reserved for local debugging. It is CPU-only, read-only, not production Swarm Inference, not P2P, and not large-model serving.
- Public Swarm Inference Alpha RC (`crowdtensor public-swarm-alpha-rc`) with `public_swarm_inference_alpha_rc_v1`, `scripts/public_swarm_inference_alpha_rc_pack.py`, and `scripts/public_swarm_inference_alpha_rc_check.py`. The `evidence-import` mode audits retained live reports for `stage0_live_requeue_evidence_ready`, `stage1_live_requeue_evidence_ready`, `public_swarm_live_requeue_evidence_ready`, `public_swarm_alpha_rc_evidence_imported`, `public_swarm_alpha_private_artifacts_absent`, and `public_swarm_inference_alpha_rc_ready`; `local-smoke` is CI-safe and does not create Kaggle resources. Retained successful proof artifacts are `dist/public-swarm-inference-alpha-live-stage0-requeue-20260527165830/public_swarm_inference_alpha.json`, `dist/public-swarm-inference-alpha-live-stage1-requeue-20260527170600/public_swarm_inference_alpha.json`, and `dist/public-swarm-inference-alpha-live-requeue-summary.json`. It is CPU-only, read-only, not production Swarm Inference, not P2P, and not large-model serving.
- Public Swarm Inference Beta (`crowdtensor public-swarm-beta`) with `public_swarm_inference_beta_v1`, `scripts/public_swarm_inference_beta_pack.py`, and `scripts/public_swarm_inference_beta_check.py`. New `public-swarm-beta product-beta` aggregates the Product RC, `session_protocol_v1`, `p2p_lite_peer_v1`, retained GPU sharded generation evidence, and local CPU fallback, requiring `public_swarm_product_beta_ready`, `public_swarm_product_rc_ready`, `coordinator_product_surface_ready`, `session_protocol_ready`, `p2p_lite_discovery_ready`, `gpu_generation_evidence_import_ready`, and `cpu_fallback_ready`. `public-swarm-beta local-loopback` and `public-swarm-beta evidence-import` remain as compatibility paths with the existing split-proof and retained Alpha RC evidence readiness codes. The command family includes `prepare`, `coordinator`, `miner`, `verify`, `collect`, and dry-run `clean`. It is Coordinator-backed, read-only, not production Swarm Inference, not libp2p/DHT/NAT traversal, not Hivemind-level serving, and not large-model serving.
- Public Swarm Product Beta (`crowdtensor public-swarm-product-beta`) with `public_swarm_product_beta_v1`, `scripts/public_swarm_product_beta_pack.py`, and `scripts/public_swarm_product_beta_check.py`. Modes `local-loopback`, `package`, and `external-existing` turn the product `serve` / `join stage0` / `join stage1` / `generate` path into an ordinary-user artifact, preserving `public_swarm_product_beta_ready`, `public_swarm_product_beta_user_path_ready`, `serve_ready`, `stage0_join_ready`, `stage1_join_ready`, `generate_ready`, `support_bundle_ready`, `private_artifacts_cleaned`, `decoded_tokens_match`, `distinct_stage_miners`, and `stage_assignment_valid`. Package mode records `private_artifacts_local_only` and `miner_join_pack_ready`; missing optional HF dependencies surface `hf_dependencies_missing`. It is CPU-only by default, read-only, Coordinator-backed, not production Swarm Inference, not libp2p/DHT/NAT traversal, and not large-model serving.
- Public Swarm Inference Beta RC (`crowdtensor public-swarm-beta-rc`) with `public_swarm_inference_beta_rc_v1`, `scripts/public_swarm_inference_beta_rc_pack.py`, and `scripts/public_swarm_inference_beta_rc_check.py`. Modes `local-loopback`, `package`, and `external-existing` aggregate Product Beta, P2P-lite route checks, CPU fallback, and product `serve` / `join` / `generate` readiness, preserving `public_swarm_inference_beta_rc_ready`, `public_swarm_product_beta_ready`, `p2p_lite_route_ready`, `p2p_lite_discovery_ready`, `cpu_fallback_ready`, `serve_join_generate_loop_ready`, `remote_generate_session_ready`, and `public_swarm_generate_ready`. Package mode records `private_artifacts_local_only` and `miner_join_pack_ready`; external-existing may record `external_runtime_verified`; missing optional HF dependencies surface `hf_dependencies_missing`. It is CPU-only by default, read-only, Coordinator-backed, not production Swarm Inference, not libp2p/DHT/NAT traversal, and not large-model serving.
- Public Swarm GPU Inference Beta (`crowdtensor public-swarm-gpu-beta`) with `public_swarm_gpu_inference_beta_v1`, `scripts/public_swarm_gpu_inference_beta_pack.py`, and `scripts/public_swarm_gpu_inference_beta_check.py`. `public-swarm-gpu-beta local-smoke` is CI-safe and emits `public_swarm_gpu_beta_smoke_ready` without claiming GPU readiness on CPU-only hosts; optional `public-swarm-gpu-beta local-loopback` selects `hf_transformers_cuda` and requires `public_swarm_gpu_beta_ready`, `gpu_runtime_ready`, `cuda_runtime_available`, `hf_transformers_cuda_ready`, `gpu_stage0_ready`, `gpu_stage1_ready`, `stage_local_partition_ready`, `stage0_partition_loaded`, `stage1_partition_loaded`, and `partition_parameter_split_valid`. `public-swarm-gpu-beta kaggle-package` emits `kaggle_gpu_package_ready`; `public-swarm-gpu-beta kaggle-auto` defaults to `--real-llm-partition-mode stage-local` for stage-owned CUDA module placement evidence. The retained successful stage-local Kaggle GPU evidence is `dist/public-swarm-gpu-beta-stage-local-live-20260528064520-shortslug/public_swarm_gpu_inference_beta_kaggle_auto.json`; it includes `stage_local_partition_ready`, `stage0_partition_loaded`, `stage1_partition_loaded`, `partition_parameter_split_valid`, `stage_gpu_memory_reduced`, and `kaggle_kernels_deleted`. The older `dist/public-swarm-gpu-beta-live-20260528-runtimepin/public_swarm_gpu_inference_beta_kaggle_auto.json` evidence remains retained as historical pre-stage-local CUDA runtime-pin proof. Generated Kaggle CUDA kernels pin `torch==2.7.1+cu118`, `torchvision==0.22.1+cu118`, and `transformers==4.40.2` for older Kaggle GPU compatibility. `public-swarm-gpu-beta evidence-import` emits `external_gpu_runtime_verified`. It is read-only optional CUDA tiny GPT evidence, not production Swarm Inference, not P2P, not a GPU pooling marketplace, and not large-model serving.
- GPU Sharded Generation Beta (`crowdtensor gpu-generate`) with `gpu_sharded_generation_beta_v1`, `gpu_sharded_generation_beta_cli_v1`, `scripts/gpu_sharded_generation_beta_pack.py`, and `scripts/gpu_sharded_generation_beta_check.py`. It wraps the optional CUDA Public Swarm GPU path with `--max-new-tokens`, supports `local-loopback`, `kaggle-auto`, and `evidence-import`, and requires multi-token generation evidence through `multi_token_generation_ready`, `gpu_sharded_generation_ready`, `generated_token_count`, and `generated_text_hash` while redacting raw generated text and generated token ids. Old single-token GPU evidence is rejected with `gpu_multi_token_generation_missing`. The retained 16-token private Kaggle GPU proof is `dist/gpu-sharded-generation-beta-kaggle-20260528095658/gpu_sharded_generation_beta_kaggle_auto.json`, with RC manifest `dist/gpu-sharded-generation-beta-kaggle-20260528095658/gpu_sharded_generation_beta_rc_manifest.json`. This is a tiny GPT CUDA multi-token Beta proof, not production Swarm Inference, not Hivemind-level serving, not P2P, not a GPU marketplace, and not large-model serving.
- Public Swarm Product RC (`crowdtensor public-swarm-product-rc`) with `public_swarm_product_rc_v1`, `scripts/public_swarm_product_rc_pack.py`, `scripts/public_swarm_product_rc_check.py`, user-facing `crowdtensor serve`, `crowdtensor join`, `crowdtensor generate`, and `crowdtensor peer`, `session_protocol_v1`, `p2p_lite_peer_v1`, `scripts/session_protocol_check.py`, `scripts/p2p_lite_discovery_check.py`, and `scripts/p2p_lite_daemon.py`. The RC imports retained GPU sharded generation evidence, verifies product CLI dry-runs, session route summaries, and P2P-lite Coordinator/stage discovery. It remains Coordinator-backed task execution, not libp2p, not DHT, not NAT traversal, not decentralized security, not Hivemind/Petals-level serving, and not large-model serving.
- Safe artifact cleanup CLI (`crowdtensor clean-artifacts`) with `cleanup_report_v1`, dry-run default, explicit `--apply`, optional `--include-reports`, generated `__pycache__` cleanup, temporary artifact cleanup, and guardrails that do not delete state or source files.
- Remote demo operator CLI (`crowdtensor remote-runbook`, `crowdtensor remote-acceptance`) with `remote_runbook_cli_v1`, `remote_acceptance_cli_v1`, default `--create-session`, fixed `--scenario-id route-baseline`, token redaction for captured command output, and explicit not production / not P2P boundaries.
- High-level remote home-compute demo CLI (`crowdtensor remote-demo prepare`, `remote-demo doctor`, `remote-demo verify`, `remote-demo collect`, `remote-demo clean`) with `remote_home_compute_demo_v1`, `remote_home_compute_doctor_v1`, `remote_home_compute_collect_v1`, `remote_home_compute_cleanup_v1`, `scripts/remote_home_compute_demo_pack.py`, `scripts/remote_home_compute_demo_check.py`, private `operator.private.env` / `miner.private.env`, `miner_join_pack_v1`, `miner_join.sh`, `MINER_JOIN.md`, dry-run cleanup defaults, read-only `POST /admin/inference-sessions` verification for `model_bundle_infer`, `remote_python_model_bundle_infer`, `remote_compute_evidence_v1`, `remote_demo_observability_v1`, token redaction, and explicit not production / not P2P boundaries.
- Remote external LLM runtime demo path (`crowdtensor remote-demo --workload external-llm`) with read-only `external_llm_infer` session creation, deterministic `--mock` and explicit operator-owned `--llm-runtime-cmd` / `--llm-runtime-url` adapter flags, `remote_python_external_llm_infer`, `scripts/remote_external_llm_evidence_pack.py`, `remote_external_llm_evidence_v1`, `remote_external_llm_observability_v1`, and redaction of raw prompts, `output_text`, runtime URL, API key, leases, and idempotency material. This is fixed-prompt runtime evidence, not public arbitrary prompt serving.
- Real two-machine CPU inference Beta rehearsal (`scripts/remote_two_machine_beta_check.py`) with `remote_two_machine_beta_check_v1`, local loopback stand-ins for the Coordinator host and Miner host, required readiness diagnosis codes `remote_two_machine_inference_ready`, `remote_two_machine_external_llm_ready`, and `remote_two_machine_beta_ready`, and 15-minute two-machine CPU inference Beta documentation. This is task-level remote CPU inference, not model sharding, not P2P, and not production Swarm Inference.
- Kaggle Remote Miner Beta target (`crowdtensor remote-demo prepare --target kaggle`) with generated `miner_join_pack_v1`, `miner_join.sh`, `MINER_JOIN.md`, `kaggle_remote_miner.py`, `kaggle_remote_miner.md`, `miner.private.env` upload guidance, `kaggle_remote_miner_prepare_ready`, `miner_join_pack_ready`, `scripts/kaggle_remote_miner_beta_check.py`, `kaggle_remote_miner_beta_check_v1`, and `kaggle_remote_miner_beta_ready`. Kaggle is treated as an outbound temporary CPU Miner environment; `operator.private.env` stays off Kaggle, GPU/TPU workload adapters are not enabled, and the path remains not production Swarm Inference and not P2P.
- Kaggle Real Runtime Acceptance (`crowdtensor remote-demo kaggle-real`) with `kaggle_real_runtime_acceptance_v1`, `scripts/kaggle_real_runtime_acceptance_pack.py`, `scripts/kaggle_real_runtime_acceptance_check.py`, generated temporary HTTP public Coordinator launch script for `24.199.118.54`, Kaggle-only upload package, `kaggle_artifacts_ready`, `coordinator_public_ready`, `kaggle_miner_seen`, `kaggle_result_accepted`, `kaggle_real_runtime_ready`, and `token_rotation_required`. The check script validates artifacts only; live success requires a real Kaggle CPU Notebook Miner. The same wrapper supports `--workload micro-llm-sharded --stage-mode split`, generating `kaggle-upload-stage0` and `kaggle-upload-stage1` packages and surfacing `kaggle_micro_llm_stage0_seen`, `kaggle_micro_llm_stage1_seen`, `kaggle_micro_llm_stage_assignment_valid`, `stage_assignment_valid`, and `kaggle_micro_llm_sharded_ready` after two live Notebook Miners complete. `scripts/kaggle_micro_llm_live_package.py` adds private Kaggle dataset/script-kernel packaging plus a temporary `--inline-kernel-payload` fallback for Kaggle input-mount issues. An artifact-backed live proof completed against `24.199.118.54:9180` with two private Kaggle CPU script kernels and `micro_llm_artifact_v1`, retaining `dist/kaggle-micro-llm-live/external-real/kaggle_real_runtime_acceptance.json` as local evidence and deleting the temporary remote Kaggle resources afterward. This keeps `operator.private.env` off Kaggle and remains CPU-only, read-only, not production, not P2P, not GPU/TPU workload execution, and not large-model sharding.
- Admin-created read-only inference session API (`POST /admin/inference-sessions`) with `inference_session_request_v1`, `task_id` result filtering, CPU `model_bundle_infer`, `scripts/admin_inference_session_check.py`, and runtime acceptance skip flag `--skip-admin-inference-session`.
- External LLM adapter smokes (`scripts/external_llm_inference_smoke.py`, `scripts/external_llm_http_adapter_smoke.py`) using `--enable-mock-llm-runtime`, optional `--llm-runtime-cmd` / `CROWDTENSOR_LLM_RUNTIME_CMD`, and OpenAI-compatible `--llm-runtime-url` / `CROWDTENSOR_LLM_RUNTIME_URL` for operator-owned local runtime wrappers.

### Known Limitations

- This is not a production DePIN network, payment system, public-internet security layer, or real LLM training platform.
- Current workloads are intentionally small and CPU-friendly so reliability behavior can be tested without GPU access.
- Browser and remote Miner paths are controlled demos; they require operator-provided transport security when used off localhost.
- P2P discovery, NAT traversal, GPU execution, WebGPU model shards, and real distributed LLM fine-tuning remain future work.

### Verification

Before publishing this alpha, maintainers should run the release flow in [docs/release.md](docs/release.md), including release gate, unit tests, runtime acceptance, release evidence, and Support Bundle generation.
